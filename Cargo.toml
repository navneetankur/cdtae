[package]
name = "cdtae"
version = "0.1.0"
edition = "2024"

[dependencies]

[lints.rust]
# to remove later
dead_code = "allow"
[lints.clippy]
pedantic = { level = "warn", priority = -1 }
correctness = { level = "warn", priority = -1 }
